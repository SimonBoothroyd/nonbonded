name: test
channels:
  - conda-forge
  - omnia
  - omnia/label/alpha
dependencies:
    # Base depends
  - python
  - pip

    # Core dependencies
  - click
  - click-option-group
  - numpy
  - pydantic
  - requests
  - pandas
  - jinja2
  - email_validator

    # Benchmark / optimization target dependencies
  - forcebalance >=1.7.5
  - openff-toolkit-base
  - openff-evaluator >=0.3.3
  - openff-recharge >=0.0.1

    # SMILES to SVG
  - rdkit

    # Plotting
  - seaborn >= 0.11

    # Statistics
  - scipy

    # REST dependencies
  - sqlalchemy <1.4.0
  - sqlalchemy-utils
