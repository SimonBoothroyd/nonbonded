name: test
channels:
  - conda-forge
  - omnia
  - openeye
dependencies:
    # Base depends
  - python
  - pip

    # Testing and development
  - pytest
  - pytest-cov
  - codecov

    # Standard dependencies
  - dash
  - forcebalance
  - mongodb
  - numpy
  - openeye-toolkits
  - pandas
  - rdkit
