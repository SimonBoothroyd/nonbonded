name: test
channels:
  - conda-forge
  - omnia
  - openeye
dependencies:
    # Base depends
  - python
  - pip

    # Testing and development
  - pytest
  - pytest-cov
  - codecov

    # Standard dependencies
  - dash
  - dash-bootstrap-components
  - fastapi
  - forcebalance
  - numpy
  - openeye-toolkits
  - pandas
  - pydantic
  - requests
  - rdkit
  - uvicorn
