name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

    # REST dependencies
  - alembic
  - fastapi
  - email-validator
  - sqlalchemy <1.4.0
  - sqlalchemy-utils
  - psycopg2
  - starlette
  - uvicorn
  - gunicorn
  - pydantic

    # Python < 3.8 backwards compatibility.
  - typing-extensions

    # Core dependencies
  - click
  - requests

   # Molecule API dependencies
  - rdkit
