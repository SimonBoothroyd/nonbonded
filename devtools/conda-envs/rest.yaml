name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

    # REST dependencies
  - alembic
  - fastapi
  - sqlalchemy
  - sqlalchemy-utils
  - psycopg2
  - starlette
  - typing-extensions
  - uvicorn
  - gunicorn
  - pydantic

    # Core dependencies
  - click
  - requests

   # Molecule API dependencies
  - rdkit
